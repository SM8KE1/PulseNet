# Experimental components

Components in this directory are excluded from public application builds and installers.

- `windows-wfp`: test-signed WFP callout driver and TCP proxy prototype for per-application bandwidth enforcement.

The public Windows implementation uses `native/windows-firewall` and does not install a kernel driver.
